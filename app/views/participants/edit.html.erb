<% content_for :title, t('helpers.titles.editing', model: @participant.human_class_name) %>

<div class="w-full">
  <h1 class="font-bold text-4xl text-primary-600">
    <%= t('helpers.titles.editing', model: @participant.human_class_name) %>
  </h1>

  <%= render "form", participant: @participant %>

  <%= link_to(
        t('helpers.anchors.back', model: @participant.human_class_name(count: 2)),
        participants_path,
        class: "ml-2 btn alt"
      ) %>
</div>
