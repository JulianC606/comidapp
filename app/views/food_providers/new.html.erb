<% content_for :title, t('helpers.titles.new', model: @food_providers.model_name.human) %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">
    <%= t('helpers.titles.new', model: @food_providers.model_name.human) %>
  </h1>

  <%= render "form", food_provider: @food_provider %>

  <%= link_to 
        t('helpers.anchors.back', model: @food_providers.model_name.human(count: 2)),
        food_providers_path,
        class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium"
      %>
</div>
